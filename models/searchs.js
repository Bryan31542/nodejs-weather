class Searchs {
  history = [];

  constructor() {
    //TODO: read from DB
  }

  async city(place = "") {
    // Peticion
    console.log(place);

    return []; // Retorna un array de objetos con los lugares
  }
}

module.exports = Searchs;
